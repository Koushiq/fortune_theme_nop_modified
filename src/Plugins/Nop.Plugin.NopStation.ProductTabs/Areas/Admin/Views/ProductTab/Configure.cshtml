@model ConfigurationModel

@{
    Layout = "_AdminLayout";
    ViewBag.PageTitle = T("NopStation.ProductTabs.Configuration").Text;
    Html.SetActiveMenuItemSystemName("ProductTabs.Configuration");
}

<form asp-controller="ProductTab" asp-action="Configure" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("NopStation.ProductTabs.Configuration")
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
        </div>
        @await Component.InvokeAsync("StoreScopeConfiguration")
    </div>

    <div class="content">
        <div class="form-horizontal">
            <div class="panel-group">
                <div class="panel panel-default">
                    <div class="panel-body">
                        <div class="form-group">
                            <div class="col-md-4">
                                <nop-override-store-checkbox asp-for="EnableProductTab_OverrideForStore" asp-input="EnableProductTab" asp-store-scope="@Model.ActiveStoreScopeConfiguration" />
                                <nop-label asp-for="EnableProductTab" />
                            </div>
                            <div class="col-md-8">
                                <nop-editor asp-for="EnableProductTab" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
