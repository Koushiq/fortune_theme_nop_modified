@model ProductTabModel

@{
    Layout = "_AdminLayout";
    //page title
    ViewBag.PageTitle = T("NopStation.ProductTabs.ProductTabs.EditDetails").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("ProductTabs");
}

<form asp-controller="ProductTab" asp-action="Edit" method="post" id="home-page-producttab-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("NopStation.ProductTabs.ProductTabs.EditDetails")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("NopStation.ProductTabs.ProductTabs.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            <button type="button" class="btn bg-red" id="producttab-delete">
                <i class="fa fa-trash-o"></i>
                @T("Admin.Common.Delete")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>
<nop-delete-confirmation asp-model-id="@Model.Id" asp-button-id="producttab-delete" />